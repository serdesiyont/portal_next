# week1

## Reverse a String

### Write a function that takes a string as input and returns the string reversed.

```python

def reverse_string(s):
    
    pass


assert reverse_string("hello") == "olleh"
assert reverse_string("") == ""
assert reverse_string("abc") == "cba"
print("All tests passed!")


```

## Sum of Array

### Write a function that takes a list of numbers and returns the sum of all elements.

```python


def sum_array(arr):
    
    pass


assert sum_array([1, 2, 3]) == 6
assert sum_array([]) == 0
assert sum_array([-1, 1, 0]) == 0
print("All tests passed!")
```


# week2
## Find Maximum Number

### Write a function that takes an array of numbers and returns the maximum value.

```typescript

function findMax(arr: number[]): number {
    // Your code here
    return 0;
}

// Test
console.assert(findMax([1, 2, 3, 4]) === 4, "Test 1 Failed");
console.assert(findMax([-1, -2, -3]) === -1, "Test 2 Failed");
console.assert(findMax([5]) === 5, "Test 3 Failed");
console.log("All tests passed!");
```

## Is Palindrome

### Write a function that checks if a given string is a palindrome (reads the same forwards and backwards).

```typescript

function isPalindrome(s: string): boolean {
    // Your code here
    return false;
}

// Test
console.assert(isPalindrome("racecar") === true, "Test 1 Failed");
console.assert(isPalindrome("hello") === false, "Test 2 Failed");
console.assert(isPalindrome("") === true, "Test 3 Failed");
console.log("All tests passed!");
```
